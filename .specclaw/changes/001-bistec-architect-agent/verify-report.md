# Verification Report: 001-bistec-architect-agent

**Change:** 001-bistec-architect-agent
**Verified:** 2026-09-23 (re-verification of commit `027a778`)
**Repository:** /Users/chandima/repos/jev-architect (branch main, no git remote configured)

**Verdict:** PARTIAL

All three gaps named in the prior PARTIAL report were genuinely remediated in commit `027a778` and confirmed with fresh evidence in this pass: the macOS bundle was actually produced and inspected, the log-redaction test genuinely captures and greps real log output, the Ollama-down edge case now disables Mode A, the all-hold edge case is now tested, and the reviews-table trigger test was already correct (prior report's "gap" on it was itself wrong, as the orchestrator's correction noted). Every regression check re-ran clean: 209 Rust tests passed (0 failed, 1 pre-existing manual-only ignore), `cargo clippy --all-targets` zero warnings, `pnpm test` 54/54, `pnpm build` succeeded, and both Playwright e2e tests passed on a fresh run. The verdict remains PARTIAL for one reason only: **AC-1's Windows/CI half is still genuinely unobtainable in this environment** — there is no git remote, so `.github/workflows/ci.yml` (including the Windows leg of NFR-1) has never executed, and this cannot be fixed by more local work.

## Per-AC Table

| AC | Verdict | Evidence |
|----|---------|----------|
| **AC-1** | **PARTIAL (macOS half now MET; Windows/CI half genuinely unobtainable here)** | `pnpm tauri build` (full bundle, no flag) was run by the orchestrator and produced `src-tauri/target/release/bundle/macos/BISTEC Architect.app` and `src-tauri/target/release/bundle/dmg/BISTEC Architect_0.1.0_aarch64.dmg`. I confirmed both exist, are dated after the commit, and are structurally real: `Contents/MacOS/bistec-architect` is a genuine `Mach-O 64-bit executable arm64` (plus `calibrate` and `catalog-check` sidecar binaries), and `hdiutil verify` reports the DMG's GPT/CRC32 checksums as **VALID**. `.github/workflows/ci.yml`'s matrix (`[macos-latest, windows-latest]`, `cargo test` / `pnpm test` / `pnpm tauri build --no-bundle`) is unchanged and still correctly written, but `git remote -v` returns nothing — the workflow has never run on GitHub, so the Windows leg of NFR-1/AC-1 remains entirely unverified in practice. This is an environment constraint (no remote to push to), not a code defect. |
| AC-2 through AC-20 (except AC-3, see below) | **MET (unchanged, re-confirmed)** | No regressions found. `cargo test` and `pnpm test` re-run clean across every test named in the prior report (catalog_check, jev_client, brief, docs_parse, pipeline rule/gate/scoring tests, questions, precedent, store, render golden files, HTML export, history replay, data-notice gate, e2e). See prior report body for per-file citations — nothing here changed in `027a778` except commands.rs/jev.rs/ollama.rs/lib.rs additions for the local-model-status command, which are additive and don't touch existing decision/scoring/routing/render logic. |
| **AC-3** | **MET (remediated)** | The two previously-passing halves (`commands.rs:api_key_is_stored_but_never_returned_by_any_command`, `store.rs:the_sqlite_file_never_contains_the_api_key`) still pass. The missing third half now exists: `src-tauri/tests/log_redaction.rs::no_log_record_ever_contains_the_api_key` installs a real `log::Log` implementation (the `log` facade, not just `tracing_subscriber`), runs a **full fake Mode A flow** through the actual command-layer functions (`set_api_key` → `ack_data_notice` → `start_describe` → `confirm_brief` → `run_decisions` → `export_adrs`/`export_report`) with a deliberately distinctive key (`sk-or-test-SECRET-LOGGREP`), plus a **real HTTP round trip** through `HttpJev` against a `wiremock` server that returns a 500 (exercising the real `Debug` impl and a real `JevError`). The capture is genuinely non-trivial: the test asserts the captured log is **non-empty**, asserts it contains the actual `"Jev request:"` debug line (proving the capture isn't a no-op), and then asserts the key substring is absent and that `HttpJev`'s Debug output is redacted (`[REDACTED]` or no `api_key` substring). This is a positive, executable proof — not "nothing logs the key" as before. I ran it directly: passes. |
| — | | |

## Edge Case / Gap Re-Checks

| Item | Verdict | Evidence |
|---|---|---|
| **Edge case: Ollama down → Mode A disabled, Mode B small still works** | **MET (remediated)** | New shared logic `probe_local_model()` (`src-tauri/src/commands.rs:281`) backs both `health_check` and the new `local_model_status` command (`commands.rs:303`, IPC-exposed at `commands.rs:614`); `local_model_status` makes **no Jev call**. `src/screens/Home.tsx` polls it and computes `localModelUnavailable = !ollama_reachable \|\| !model_present`; the Describe button is disabled (`canSubmitDescribe` requires `!localModelUnavailable`) with the exact `ollama pull …` command shown when down, while the Upload button has no such gate (Mode B stays enabled). `Home.test.tsx` covers this (43 new/changed lines). Fixture `local_model_status.json` added for the IPC contract test (`src/lib/contract.test.ts`). |
| **Edge case: every option on hold → `no_eligible_options`** | **MET (remediated)** | `src-tauri/tests/pipeline.rs::every_option_on_hold_is_skipped_with_no_eligible_options_and_never_asked` forces every option of `document-db` to `Ring::Hold` via a mutated catalogue clone, runs the real pipeline, and asserts: zero `choice__document-db`/`score__document-db` requests were ever sent; the type is absent from the report's decisions *and* absent from `not_applicable` (i.e., correctly distinguished from "not applicable" — it's applicable but has nothing eligible to decide); and the persisted `decisions` stage result's `skipped[]` array has an entry for `document-db` with `reason == "no_eligible_options"`. Passes. |
| **Reviews DB trigger (prior report's flagged "gap")** | **Confirmed the orchestrator's correction was right; no gap** | `src-tauri/src/store.rs:698 reviews_table_rejects_raw_update_and_delete` inserts a session, a decision, and one review row, then issues a raw `conn.execute("UPDATE reviews SET reviewer = ...")` and a raw `conn.execute("DELETE FROM reviews")` directly against the SQLite connection and asserts both `Result`s are `Err` — i.e., it does exercise the DB trigger itself, not just the insert-only code path. The prior report's gap #6 (second half) was incorrect, as the orchestrator noted; AC-13 has no gap. |
| **NFR-6 (≤60s report generation)** | **Still UNVERIFIED — unchanged** | No benchmark or timing assertion exists anywhere in the tree (`grep` for perf/timing/duration assertions in `src-tauri/tests` and `golden/` returns nothing new). `golden/calibration-report.md` (from `cargo run --bin calibrate`, 11 cases, live API) reports **accuracy, cost ($0.0149 total), input tokens (353,945), and route-agreement grids across threshold/margin combinations — it contains no per-case or aggregate wall-clock timing at all.** It proves the pipeline runs end-to-end successfully against the live API for 11 real cases (and surfaces one calibration note: `04-realtime-dashboard-node` expected `fastify-ts` but got `rest-api-node`, p=0.42 — a weighting/calibration follow-up, not a defect), but it says nothing about the ≤60s NFR-6 budget one way or the other. This remains a genuine, unaddressed gap; it is an NFR rather than a lettered AC, so it does not by itself change the PASS/PARTIAL determination, but it must be named explicitly per instructions. |

## Fresh Regression Evidence (this pass)

- `cargo test --manifest-path src-tauri/Cargo.toml`: **209 passed, 0 failed, 1 ignored** (the ignored test is the pre-existing `manual_real_extraction_against_local_ollama`, explicitly gated as requiring a live local Ollama — not a regression).
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets`: clean, zero warnings (confirms the `field_reassign_with_default` fix in `store.rs` mentioned in the commit message actually landed).
- `pnpm test` (Vitest): **14 test files, 54 tests, all passed**.
- `pnpm build`: succeeded (`tsc -b && vite build`, output to `dist/`).
- `pnpm exec playwright test`: **2 passed** — `Settings health panel › shows connection status...` (1.9s) and `BISTEC Architect smoke test (AC-18) › Describe → Brief → Run → Report → Accept → export enabled` (3.7s).
- `ls`-confirmed bundle artifacts: `src-tauri/target/release/bundle/macos/BISTEC Architect.app` (real arm64 Mach-O executable) and `src-tauri/target/release/bundle/dmg/BISTEC Architect_0.1.0_aarch64.dmg` (12.5 MB, `hdiutil verify` reports VALID checksums).

## Remaining Gaps

1. **Windows build/test/CI (NFR-1 / AC-1's second half) is unverified.** `.github/workflows/ci.yml` is correctly written but has never executed anywhere — no git remote exists in this repository. This is not fixable by further local work; it requires an external action.
2. **NFR-6 (≤60s report generation) has zero measurement**, synthetic or live. The 11-case live calibration run proves functional correctness end-to-end against the real API but records no timing data at all.
3. (Informational, not a gap) The live calibration surfaced one expected-type mismatch (`04-realtime-dashboard-node`: got `rest-api-node`, expected `fastify-ts`, p=0.42) — a calibration/weighting tuning note, not a code defect.

## Next Actions

1. **Push this repository to a real remote (GitHub) and let `.github/workflows/ci.yml` run at least once**, so the Windows leg of NFR-1/AC-1 gets real signal instead of remaining "correctly written but never executed." Owner: repo owner/orchestrator — requires a decision about where this project is hosted, outside what a build/verify agent can do locally.
2. Optionally add a lightweight synthetic timing assertion (e.g., wall-clock around the fake-Jev pipeline test) or a documented manual-QA timing step for NFR-6, since it currently has no coverage of any kind. Not blocking for PASS on the lettered ACs, but worth closing before a real release claim about performance.
3. When live-API budget allows, investigate the `04-realtime-dashboard-node` → `rest-api-node` vs. expected `fastify-ts` calibration mismatch noted in `golden/calibration-report.md` (p=0.42, a genuine near-miss) as a weighting/threshold tuning task, not a bug fix.

---
**Orchestrator correction:** the calibration note is misstated above — for `04-realtime-dashboard-node` the `rest-api-node` decision type was judged *not applicable* (p=0.42) and never decided; it did not choose a wrong option. All 25 decided expectations were correct.
