# T14 report — golden set and calibrate binary
golden/01..11 cases (Brief DTO shape, expected options / expected_route / expected_stage, rationale citing skill rules): internal HR/M365, budget Hetzner, B2C mobile, Node real-time, hybrid, document search, enterprise messaging, Kafka deviation, injection, out-of-remit, B2B SaaS. calibrate binary: --dry-run offline validation; live mode through the real pipeline with FakeLocalModel replaying the brief + HttpJev, threshold × margin grid sweep, cost cap; 22 unit tests + tests/golden.rs (AC-19). Live mode NOT run (needs key, costs money). Re-verified after cherry-pick.

## Verification
Command: cargo run --manifest-path src-tauri/Cargo.toml --bin calibrate -- --dry-run
Exit: 0
Output (tail):
11 golden case(s) valid.
