# T10 report — Tauri command surface
commands.rs: AppState, AppError {code,message,stage?}, ClientFactory (LiveClients: HttpJev / NoKeyJev, OllamaModel), plain testable fns + thin #[tauri::command] wrappers (commands::ipc) for all 20 contract commands, EventSink for pipeline://progress, notice + key gates, review via settings.reviewer_name, ADR/report export with adr numbers. lib.rs registers state + handlers. Cargo.toml: default-run added (T2's extra bin broke `tauri build`). 7 command tests (AC-3, AC-13, AC-17, no-key, exports) + contract fixtures + vitest Zod contract test. Orchestrator follow-up: fixtures regenerate only with UPDATE_CONTRACT_FIXTURES=1; CI runs Rust tests (with it) before pnpm test.

## Verification
Command: UPDATE_CONTRACT_FIXTURES=1 cargo test --manifest-path src-tauri/Cargo.toml && pnpm test && pnpm tauri build --no-bundle
Exit: 0
Output (tail):
test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.08s
 Test Files  14 passed (14)
      Tests  50 passed (50)
Built application at: src-tauri/target/release/bistec-architect
