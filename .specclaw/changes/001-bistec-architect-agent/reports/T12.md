# T12 report — brief review and run progress screens
Brief.tsx (editable draft, dirty indicator, save, confirm → run, confirm disabled on empty summary, upload two-column with SectionList + citation highlight), BriefEditor (context selects with skill wording + unknown flag, compliance chips, editable lists with source chips, tech tags), SectionList, Run.tsx (progress subscription, stage stepper, out_of_remit panel, error + Retry, no_api_key / data_notice handling). ipc-mock: additive failNextRunDecisions switch. Re-verified after cherry-pick together with T13.

## Verification
Command: pnpm test && pnpm build
Exit: 0
Output (tail):
 Test Files  13 passed (13)
      Tests  38 passed (38)
✓ built
