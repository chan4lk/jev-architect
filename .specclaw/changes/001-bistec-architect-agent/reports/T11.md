# T11 report — UI foundation, IPC layer, settings, home
schemas.ts (Zod for every contract DTO + AppError), ipc.ts (only @tauri-apps importer; real vs mock by __TAURI_INTERNALS__), ipc-mock.ts (seeded report with 9 decisions, brief session, validation, progress timer, resetMock/setHealthOverride), AppShell, Home (Describe/Upload + history), Settings (write-only key, thresholds, weights with uncalibrated note, reviewer), DataNotice, HealthPanel (pull command). Ring colour CSS vars. Added @tauri-apps/plugin-dialog + shadcn alert. Re-verified after cherry-pick.

## Verification
Command: pnpm test && pnpm build
Exit: 0
Output (tail):
 Test Files  5 passed (5)
      Tests  12 passed (12)
✓ built
