# T13 report — report screen, review actions, export
Report.tsx (filters, cards, not-applicable, export bar with unreviewed warning, print), DecisionCard, RingBadge, ProbBars, ConfidenceMeter, ScoreHeatmap, ReviewDialog (Accept/Override/Reject validation, reviewer gating), ReportSummary (counts, cost/n/a, banners, uncalibrated note), report.print.css. 8 screen tests + component unit tests. ipc-mock unchanged. Re-verified after cherry-pick.

## Verification
Command: pnpm test && pnpm build
Exit: 0
Output (tail):
 Test Files  11 passed (11)
      Tests  32 passed (32)
✓ built
