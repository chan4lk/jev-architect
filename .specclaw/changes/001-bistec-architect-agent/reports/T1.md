# T1 report — Scaffold Tauri 2 + React app, tooling, CI

Scaffolded via `pnpm tauri init` (@tauri-apps/cli 2.11.5, not global cargo-tauri 1.5). React 19 + TS strict + Tailwind v4 + shadcn (12 components) + Zod 4 + TanStack Query + react-router + Vitest + Playwright (no browsers). Rust crate `bistec-architect` / lib `bistec_architect`, tauri-plugin-dialog registered, identifier `global.bistec.architect`, CSP per design. CI matrix macos-latest + windows-latest.

## Verification
Command: pnpm test && cargo test --manifest-path src-tauri/Cargo.toml && pnpm tauri build --no-bundle
Exit: 0
Output (tail):
   Compiling bistec-architect v0.1.0 (/Users/chandima/repos/jev-architect/src-tauri)
    Finished `release` profile [optimized] target(s) in 14.51s
       Built application at: /Users/chandima/repos/jev-architect/src-tauri/target/release/bistec-architect
