# T15 report — Playwright smoke test and README
playwright.config.ts (chromium, vite preview webServer), e2e/smoke.spec.ts (AC-18 full flow Describe → Brief → Run → Report → Accept → export enabled; health panel test), README.md (prereqs, data notice, dev/build/test, catalog-check, calibrate, pipeline, layout, limitations). Re-verified on build branch after cherry-pick. Full-suite + tauri build re-run happens at finalize after T10/T14 land.

## Verification
Command: pnpm e2e
Exit: 0
Output (tail):
  2 passed
